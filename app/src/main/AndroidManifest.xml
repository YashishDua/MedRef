<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.hackbvp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity" >

        </activity>
        <activity
            android:name=".SelectLangActivity"
            android:label="@string/title_activity_select_lang"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SensorActivity" />
        <activity android:name=".CarSlotsActivity" />
        <activity
            android:name=".TrainingActivity"
            android:label="@string/title_activity_training"
            android:theme="@style/AppTheme.NoActionBar">

            </activity>
        <activity
            android:name=".SplashScreen"
            android:label="@string/title_activity_splash_screen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SelectParkingArea"
            android:label="@string/title_activity_select_parking_area"
            android:theme="@style/AppTheme.NoActionBar"/>
    </application>

</manifest>